<h1>New Message<h1>

<%= form_with(model: [ @room, @message ]) do |form| %>
  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
    <%= form.submit "Send" %>
  </div>
<% end %>

<%= link_to 'Back', room_path(@room) %>